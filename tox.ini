; [tox]
; envlist = py39
; skipsdist = True
; [testenv]
; deps =
;     coverage
; commands =
;     coverage run -m unittest
;     coverage xml
; [coverage:run]
; relative_files = True
; source = ./src/
; branch = True

[tox]
envlist = py39
skipsdist = True
[testenv]
deps =
    pytest
    coverage
commands =
    coverage run -m pytest
    coverage xml
[coverage:run]
relative_files = True
source = ./
branch = True


